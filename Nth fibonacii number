#include<bits/stdc++.h>
using namespace std;
#define ll long long

int fact(int n)
{
    if(n==0)
        return 0;
    else if(n==1)
        return 1;
    else
    {
        int res= fact(n-1)+fact(n-2);
        return res;
    }
}

int main()
{
    int n;
    cin>>n;

    cout<<fact(n);
}



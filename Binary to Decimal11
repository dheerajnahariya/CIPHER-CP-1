#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main()
{
    ll num;
    cin>>num;
    int sum=0;
    int i=0;

    while(num>0)
    {           int tem=num%10;

        sum=sum+(pow(2,i)*tem);
        i++;
        num=num/10;

    }
    cout<<sum;
}
